async function load(){try{let r=await fetch("https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum,tether&vs_currencies=usd");let d=await r.json();document.getElementById("marketPrice").textContent=`BTC: $${d.bitcoin.usd} | ETH: $${d.ethereum.usd} | USDT: $${d.tether.usd}`;}catch(e){document.getElementById("marketPrice").textContent="Unable to load prices.";}} load(); setInterval(load,30000);